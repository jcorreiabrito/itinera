"""Deferred feature stubs (NOT wired into the app).

Decision D10 defers auto-import from booking emails / PDF parsing to a later
phase. This package is a clearly-marked placeholder so the future home for that
code is obvious; nothing here is imported by the running service.
"""